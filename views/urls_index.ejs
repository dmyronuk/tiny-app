<!DOCTYPE html>
<html lang="en">
  <head>
    <% include ./partials/_header.ejs %>
  </head>
  <body>
    <div>
      <table>
        <tr>
          <th>URL</th>
          <th>Short</th>
          <th>Link</th>
        </tr>

        <% Object.keys(urls).forEach(url => { %>
          <tr>
            <td><%= urls[url] %></td>
            <td><%= `/u/${url}` %></td>
            <td><%- `<a href=/u/${url}>Link</a>` %></td>
          </tr>
        <% }); %>

      </table>
    </div>
  </body>
</html>
